snapshot:
  url: https://raw.githubusercontent.com/commercialhaskell/stackage-snapshots/master/lts/24/38.yaml

# Backend is flavor-neutral — building it standalone won't work
# (no Types in scope). Verification happens through the consumer
# projects under dist-ffi/test-{vanilla,docking}/, which set the
# right flag and pull in the matching core.
packages:
- .
